[workspace]
members = [
    "hdspace-common",
    "hdspace-models",
    "hdspace-renew",
    "openviking-common",
    "openviking-memory",
]
resolver = "2"

[workspace.metadata]

[profile.dev]
panic = "abort"

[profile.release]
opt-level = "z"
lto = true
strip = true
panic = "abort"
